import { useParams } from 'react-router-dom';
import { Breadcrumbs } from '@/shared/index';
export const ProductDetailPage = () => {
  return (
    <div>
      <Breadcrumbs Title="ProductDetailPage" />
      ProductDetailPage
    </div>
  );
};
